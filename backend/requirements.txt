# The ParticleWave toolchain, from PyPI. Pinned exactly rather than floored:
# the engine JS this service serves at /engine ships inside this wheel, so the
# pin decides which build the browser gets, not just which extractor runs.
#
# Until 1.4.0 this was a relative path to a wheel vendored in vendor/, built by
# `scripts/sync_wheel.py`. That script is still the way to test an unreleased
# build — it drops a wheel in vendor/ and repins this line at it — but the
# released path is PyPI. Nothing in service/ changes either way; it only ever
# imports the package by name.
particle-wave==1.4.0

# Faster Canny and proper CLAHE. Optional for the pipeline, which falls back to
# scipy and Pillow, but the fallback is markedly slower.
opencv-python-headless>=4.8,<5

fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
python-multipart>=0.0.18
